/**
 * Constants for the TaxDetails module.
 * 
 * @deprecated This file is maintained for backward compatibility.
 * Please import directly from './config' for new code.
 */

// Re-export all constants from centralized config
export {
  TAX_POLICY_CODES,
  TAX_ROW_LABELS,
  TAX_LABEL_CLASSES,
  type TaxPolicyCode,
} from './config';
