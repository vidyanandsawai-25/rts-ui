/**
 * @deprecated Use the full module at `./search-property` instead.
 * Kept as a thin re-export for backwards compatibility with any
 * lingering imports of `@/components/modules/property-tax/PropertySearch`.
 */
export { PropertySearch as default, PropertySearch } from "./search-property";
