import { createFloorRangeAction } from '../../../../../modules/property-tax/floor-master/floorMaster.action';\ndescribe('createFloorRangeAction duplicate range', () => {\n  it('should return duplicate error if range exists', async () => {\n    const result = await createFloorRangeAction({ rangeFrom: '11', rangeTo: '18', prefix: 'A5FL', suffix: '' });\n    expect(result.success).toBe(false);\n    expect(result.statusCode).toBe(409);\n    expect(result.messageKey).toMatch(/duplicate/i);\n  });\n});
