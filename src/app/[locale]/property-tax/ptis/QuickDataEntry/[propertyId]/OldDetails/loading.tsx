import { LoadingPage } from '@/components/common';

export default function OldDetailsLoading() {
  return (
    <LoadingPage
      translationNamespace="quickDataEntry.oldDetails.loading"
    />
  );
}
