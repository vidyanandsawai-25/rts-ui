import { LoadingPage } from '@/components/common';

export default function EditPolicyConfigurationLoading() {
  return (
    <LoadingPage
      translationNamespace="policyConfiguration.loading"
    />
  );
}
