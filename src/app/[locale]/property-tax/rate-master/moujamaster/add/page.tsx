import { MoujaForm } from "@/components/modules/property-tax/mouja-master";
import React from "react";

export default async function AddPage(): Promise<React.ReactElement> {
  return <MoujaForm id={null} initialData={undefined} />;
}
