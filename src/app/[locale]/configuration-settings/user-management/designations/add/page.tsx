import { DesignationFormWrapper } from './DesignationFormWrapper';

export const dynamic = 'force-dynamic';

export default async function AddDesignationPage() {
  return <DesignationFormWrapper />;
}
